# Gemfile for OS Migrate AsciiDoc documentation

source 'https://rubygems.org'

gem 'asciidoctor', '~> 2.0'
gem 'rouge', '~> 4.1'

# Required explicitly: logger was removed from Ruby default gems in Ruby 3.5+,
# and asciidoctor relies on it.
gem 'logger'